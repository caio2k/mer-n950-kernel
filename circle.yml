machine:
  services:
    - docker

dependencies:
  pre:
    - mkdir -p ~/input ~/output
  override:
    - docker run -ti -v ~/input:/srv/mer/targets/n950rootfs/root/input -v ~/output:/srv/mer/targets/n950rootfs/root/output caio2k/mer-n950-kernel:latest nemomobile kernel-adaptation-n950-n9 n9_mer_defconfig

test:
  override:
    - ls ~/input
